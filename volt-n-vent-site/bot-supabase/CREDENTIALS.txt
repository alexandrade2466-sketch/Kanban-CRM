JNB Services / Volt N' Vent — bot credentials file
================================================

File to use (in this folder):
  .env.jnb-voltnvent.local

Copy from example if needed:
  .env.jnb-voltnvent.example

Your Supabase project:
  Name: JNB Services / Volt N' Vent
  ID:   emrqlssbrntnvvdwwojs

Fill in SUPABASE_DB_PASSWORD only (from Supabase → Settings → Database).
You do NOT need anon or publishable API keys to run the schema.

Then tell the assistant: "password is in jnb voltnvent env, run schema"

Run schema yourself:
  powershell -File scripts\Run-VoltNVent-BotSchema.ps1
