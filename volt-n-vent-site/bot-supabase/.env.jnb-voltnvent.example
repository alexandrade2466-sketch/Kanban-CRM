# Copy to .env.jnb-voltnvent.local (same folder — gitignored)
# Supabase → JNB Services / Volt N' Vent → Settings → Database → password

SUPABASE_PROJECT_REF=emrqlssbrntnvvdwwojs
SUPABASE_URL=https://emrqlssbrntnvvdwwojs.supabase.co
SUPABASE_DB_PASSWORD=your_database_password_here

# Optional for n8n (Settings → API → service_role — not anon key)
# SUPABASE_SERVICE_ROLE_KEY=

# Optional for GHL (edit 002_seed or set here)
# GHL_LOCATION_ID=
